import React from 'react';
import { MobileNav, MobileNavBar } from './MobileNav';

/**
 * DEPRECATED: Use MobileNav from './MobileNav' instead.
 * This is a temporary compatibility layer to ease migration.
 */
export const MobileNavigation = MobileNav;

/**
 * DEPRECATED: Use MobileNavBar from './MobileNav' instead.
 * This is a temporary compatibility layer to ease migration.
 */
export { MobileNavBar };

export default MobileNavigation; 